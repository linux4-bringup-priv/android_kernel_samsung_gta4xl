name: Kernel Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Repo'yu klonla
      uses: actions/checkout@v4

    - name: Gerekli bağımlılıkları yükle
      run: |
        sudo apt update
        sudo apt install -y bc bison flex libssl-dev make \
          python3 python-is-python3 git zip unzip \
          ccache libncurses5-dev libncursesw5-dev \
          clang lld llvm llvm-dev wget curl

    - name: build.sh çalıştır
      run: |
        chmod +x build.sh
        ./build.sh

    - name: Derleme çıktısını listele
      run: ls -al out/arch/arm64/boot/ || echo "Image bulunamadı"

    - name: Image dosyasını artifact olarak yükle
      uses: actions/upload-artifact@v4
      with:
        name: Kernel-Image
        path: out/arch/arm64/boot/Image
